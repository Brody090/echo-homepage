# wrangler.toml — Echo's Homepage (Cloudflare Pages)
# 本文件用于 Wrangler CLI 部署（wrangler pages deploy），
# 不影响 Cloudflare Pages 的 Git 集成自动部署配置。
name = "echo-homepage"
pages_build_output_dir = "dist"
compatibility_date = "2026-08-04"
